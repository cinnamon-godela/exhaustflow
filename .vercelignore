# Don't deploy the Python chiller API (PyTorch ~7GB). Only api/chiller-predict.js is deployed (proxy to EC2).
api/main.py
api/requirements.txt
api/README.md

# Build artifacts and caches (optional, keeps deploy lean)
node_modules/.vite
dist
