# Don't deploy the Python chiller API to Vercel (it uses PyTorch ~7GB).
# The app calls the API on AWS EC2 instead.
api/

# Build artifacts and caches (optional, keeps deploy lean)
node_modules/.vite
dist
